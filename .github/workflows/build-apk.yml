name: Build Android APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Java (JDK 21)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install npm dependencies
        run: npm ci

      - name: Build Angular (production)
        run: npx ng build --configuration=production

      - name: Verify Angular build output
        run: |
          echo "=== dist/ ==="
          ls -la dist/ || true
          echo "=== dist/weather-app/ ==="
          ls -la dist/weather-app/ || true
          echo "=== dist/weather-app/browser/ ==="
          ls -la dist/weather-app/browser/ || true
          echo "=== capacitor versions ==="
          npx cap --version || true
          cat package.json | grep -A1 '"@capacitor'

      - name: Sync Capacitor with Android
        run: npx cap sync android
        env:
          CI: 'true'

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: weather-demo-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
