import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demo.weatherapp',
  appName: 'Weather Demo',
  webDir: 'dist/weather-app/browser'
};

export default config;
